make a header "mytunes" or whatever
build a form with an <input> where a user can search 
search button (or enter) that makes the search request to the API 
^^ start here like with the ajax example



reset results
response for no results
handle responses from the server that are not in the 200 range

css